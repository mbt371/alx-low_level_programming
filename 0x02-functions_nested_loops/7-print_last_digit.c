#include "main.h"

/**
* print_last_digit - a function that prints the last digit
* @n: n is an integer
*
* Return: integer
*/

int print_last_digit(int n)
{
int lastDig;


if (n >=  0)
{
latDigit = n % 10;
}
else
{
lastDigit = (n % 10) * -1;
}
_putchar('0' + lastDig);
return (lastDig);
reture (lastDig)
}

